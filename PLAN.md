# 費半 ETF (00830) 即時淨值 — macOS 選單列 App 實作 Plan（大方向）

> **Repo 名稱：`navbar-00830`**（NAV + menu bar 雙關，含標的代號；可自行更換）
> 這份是產品／架構層級的方向文件。技術選型、函式庫、資料結構、實作細節交由 Claude Code 評估決定。
> **交付形式：macOS 選單列（menu bar）App，自己單人使用。** 計算邏輯與資料源策略不變，只有呈現層是選單列。

---

## 1. 目標

一個**常駐 macOS 選單列的 App**，掃一眼就看到 00830（國泰費城半導體）的：

- 目前**盤後重估即時淨值**（用美股半導體 ETF 盤後價換算）
- 目前**盤中市價**
- 兩者的**折溢價 %**，並在折價超過設定門檻時**顏色標示**（純呈現，不做主動推播）

用途：盤中判斷 00830 相對真實淨值是便宜還是貴，輔助進場決策。

**呈現設計：**

- **選單列本體**：常駐顯示折溢價，例如 `00830 −1.0%`；折價超門檻變紅、溢價變綠（顏色依偏好）。
- **點開 popover**：顯示完整明細 —— 盤後重估淨值、即時市價、折溢價、各數字時間戳與來源、目前市場時段、三代理（SOXX/SOXQ/SOXL）交叉值。
- **背景常駐刷新**：不需開視窗、不需開瀏覽器；App 在背景定時更新（刷新頻率分層見 §5、§3）。
- 自用即可，無需 code signing／notarization（首次於「系統設定 → 隱私權與安全性」按允許即可）。

---

## 2. 核心計算邏輯（本專案的靈魂，務必正確）

### 2.1 基本公式

```
盤後重估即時淨值 = 最新官方每日淨值 × (1 + 半導體 ETF 盤後漲跌%) × 匯率調整因子
折溢價 % = (00830 市價 / 盤後重估淨值) − 1        # 負值 = 折價
```

### 2.2 三個輸入的來源與意義

| 輸入 | 意義 | 來源策略 |
|---|---|---|
| 最新官方每日淨值 | 已反映「上一個美股收盤」的基準值 | 國泰投信官網每日淨值 |
| 半導體 ETF 盤後漲跌% | 收盤後到盤後結束的增量變動（代理 SOX 盤後） | **SOXX 為主**，SOXQ / SOXL 交叉驗證 |
| 匯率調整因子 | 官方淨值定價時點到現在的 USD/TWD 變動 | 台幣即期匯率 |

### 2.3 代理法選擇（已確認採用）

用單一美股半導體 ETF 的盤後漲跌%當作整籃 SOX 的盤後變動代理，**不做 30 檔成分股逐檔重估**。

- **主來源 SOXX**：流動性最好、盤後報價穩定。注意 SOXX 追蹤 ICE Semiconductor，與 00830 的 PHLX SOX 有微小成分差異。
- **交叉驗證 SOXQ**：Invesco PHLX Semiconductor ETF，追蹤的正是 PHLX SOX（與 00830 同指數），但盤後流動性較差、報價可能不連續 → 當輔助校對，不當主值。
- **SOXL（3x）**：盤後成交更活，可用「SOXL 盤後% ÷ 3」再交叉檢查一次。

> 取捨：代理法犧牲少許精確度（±0.3~0.5%）換取極低的維護成本與穩定性。若日後要更準，再升級成分股逐檔重估（見 §7）。

---

## 3. 資料時序模型（最容易做錯的地方，請 Claude Code 特別處理）

台股與美股有時區交錯，程式必須依「現在處於哪個時段」決定要抓什麼、以及數字是否凍結：

| 現在時段（台北） | 美股狀態 | 盤後 ETF 報價 | 儀表板該顯示 |
|---|---|---|---|
| 09:00–13:30（台股盤中） | 已收、盤後也已結束 | **凍結**在今早 08:00 的盤後收盤 | 用凍結盤後值算 iNAV；市價即時跳動 |
| 13:30–隔日 04:00 | 美股未開或正常盤/盤後進行中 | 視情況 | 台股已收，僅供參考 |
| 04:00–08:00（台北清晨） | 美股盤後進行中 | **即時跳動** | 盤後值會變（一般使用者少看） |

**關鍵洞察**：使用者真正能下單的台股盤中時段，美股價格是**凍結**的，盤後 ETF 報價不會動，iNAV 在盤中唯一的變數只有台幣匯率。所以「即時」的重點其實是**即時抓 00830 市價 + 正確鎖定今早 08:00 定格的盤後值**，而不是高頻刷美股。

**還要處理**：美股/台股休市日、台股半天交易、日期換算（哪一筆才是「最新官方淨值」對應「最新美股收盤」）。

---

## 4. 系統架構大方向

```
┌─────────────────────────────────────────────┐
│  呈現層：macOS 選單列 App                      │
│  - 選單列本體：常駐顯示折溢價 %（超門檻變色）  │
│  - popover：iNAV / 市價 / 折溢價 / 時間戳 /    │
│             市場時段 / 三代理交叉值            │
│  - 背景定時觸發刷新（分層頻率）               │
└───────────────▲─────────────────────────────┘
                │ 呼叫計算層取得彙整數據
┌───────────────┴─────────────────────────────┐
│  計算層（核心，與呈現形式無關）                │
│  - 抓 4 個來源、做時序判斷、算 iNAV/折溢價     │
│  - 快取 + 失敗容錯                              │
└──┬─────────┬──────────┬──────────┬───────────┘
   │         │          │          │
 官方淨值  SOXX盤後   台幣匯率   00830市價
```

- **關鍵原則：計算層與呈現層徹底分離。** 計算層（抓取 → 時序判斷 → 計算 → 快取容錯）是純邏輯，跟「選單列 / 網頁 / CLI」哪種殼無關。日後若想改回網頁或加告警，只換殼、計算層不動。
- **呈現層（選單列）**：定時向計算層要最新結果並更新選單列文字與 popover。刷新頻率分層 —— 台股盤中 00830 市價可較頻繁，美股盤後值在台股盤中凍結、低頻即可（見 §3）。
- **資料源介接**：四個來源都是網頁報價，抓取方式（API / 網頁解析）由 Claude Code 評估各來源穩定度後決定。選單列 App 直接在本機抓取，沒有瀏覽器 CORS 限制（比原網頁版少一層顧慮）。

---

## 5. 呈現內容（最小可用）

**選單列本體（一直看得到）：**

- 常駐顯示折溢價 %，例如 `00830 −1.0%`
- 折價超過使用者設定門檻（例如 −3%）→ 文字變色高亮
- 資料過期或抓取失敗時給明確視覺提示（例如變灰或加問號），避免看到過期數字誤判

**點開 popover（要細節時）：**

- **盤後重估即時淨值**、**00830 即時市價**、**折溢價 %**
- 每個數字附**資料時間戳 + 來源**（讓使用者判斷新鮮度）
- 狀態列：目前時段（台股盤中／美股盤後／休市）＋ 各來源抓取成功與否
- SOXX / SOXQ / SOXL 三者換算出的 iNAV 並列，看彼此差異當可信度參考
- 折溢價門檻可在 popover 內設定

**刷新頻率分層**（省電、避免無謂抓取）：台股盤中對 00830 市價較頻繁；美股盤後值在台股盤中凍結 → 低頻或只在時段切換時抓一次。

---

## 6. 關鍵取捨與風險（要在 UI 上誠實揭露）

- **代理法誤差**：SOXX≠PHLX SOX，盤後有 ±0.3~0.5% 偏差。UI 應把 iNAV 呈現為「估計值」而非精確值，最好給區間。
- **盤後流動性薄**：盤後最後幾筆可能是薄量跳動價，需檢查時間戳是否為 20:00 ET 收尾，避免抓到雜訊價。
- **匯率時點**：官方淨值用的參考匯率與即時匯率不同步，是誤差來源之一，但量級小（<0.5%）。
- **折價 ≠ 套利**：台股盤中鎖定的折價本質是市場對「今晚美股」的預期定價，無法對沖隔夜 gap。UI 不應暗示「折價=穩賺」，避免誤導。
- **官方基準**：國泰投信官網有每 15 秒的官方盤中預估淨值，可當校準基準（但需點同意聲明、且抓取條款需 Claude Code 評估合規性）。

---

## 7. 未來可延伸（先不做，列給 Claude Code 知道方向）

- 精度升級：改成前 10~15 大成分股逐檔盤後價 × 權重重估，剩餘用收盤價補。
- 歷史折溢價分布：把每日折溢價存起來，動態算出「這檔的正常波動帶」，讓門檻不是拍腦袋而是統計分位數。
- 主動告警：折價超門檻推播（LINE/Telegram/Email）——目前版本只做儀表板顯示。

---

## 8. 交給 Claude Code 評估的技術決策點

1. 四個資料源各自用什麼方式取得最穩（官方 API vs 網頁解析 vs 第三方報價服務），以及抓取的合規／頻率限制。
2. **選單列 App 技術路線擇一**（取捨見下）：
   - **Swift + SwiftUI（MenuBarExtra）**：最原生、最省電、常駐體驗最好；Web 技能無法重用，需另寫。
   - **Tauri（選單列 tray 模式）**：可重用 Web 版畫面與邏輯、輕量；比 Electron 省資源。
   - **Python + rumps**：最快做出可用雛形，適合自用、不在意打包精緻度。
   - 選型主軸：開發速度 vs 原生體驗／省電；是否日後要重用 Web 版。
3. 時序狀態機的實作與各市場行事曆（休市日）資料怎麼來。
4. 快取策略與資料源失效時的 fallback（例如 SOXX 掛了改用 SOXQ）。
5. 刷新頻率的分層設計（即時市價 vs 凍結的盤後值），並考量選單列 App 背景常駐的省電。
6. 官方 iNAV 校準要不要做、以及抓取的合規評估。
7. 開機自動啟動（login item）與設定值（門檻、顏色）持久化方式。

> 打包：自用免簽章／公證，首次於「系統設定 → 隱私權與安全性」允許即可；要對外散佈才需處理 Apple Developer 簽章與 notarization。

---

## 附錄：目前已驗證的計算範例（2026/07/07 盤中實測）

```
最新官方淨值(7/6)      = 91.68
SOXX 盤後漲跌          = −0.95%   (581.51 → 576.00)
盤後重估即時淨值       = 91.68 × (1 − 0.95%) ≈ 90.8
00830 即時市價         = 89.90
折溢價                 = 89.90 / 90.8 − 1 ≈ −1.0%（折價，落在正常帶內）
```
這組數字可當實作完成後的 sanity check 基準。
